"use strict";var https=require("https"),fs=require("fs"),express=require("express"),app=express(),options={key:fs.readFileSync("key.pem"),cert:fs.readFileSync("cert.pem")};https.createServer(options,app).listen(3e3,function(){console.log("Server running on https://localhost:3000")});
//# sourceMappingURL=server.min.js.map
